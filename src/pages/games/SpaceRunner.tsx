import { useCallback, useEffect, useRef, useState, type CSSProperties } from 'react';
import { motion } from 'framer-motion';
import { ArrowDown, ArrowLeft, ArrowRight, Pause, Play, Rocket, Sparkles, Zap } from 'lucide-react';
import GameShell, { type GameProps } from './GameShell';

type Lane = 0 | 1 | 2;
type RunnerState = 'playing' | 'paused' | 'won' | 'lost';
type ObstacleKind = 'meteor' | 'barrier' | 'drone';
type PickupKind = 'coin' | 'star' | 'energy';
type Obstacle = { id: number; lane: Lane; y: number; kind: ObstacleKind; phase: number };
type Pickup = { id: number; lane: Lane; y: number; kind: PickupKind; phase: number };
type Burst = { id: number; lane: Lane; y: number; emoji: string };

const LANES: Lane[] = [0, 1, 2];
const GAME_LENGTH = 72;
const START_SPEED = 31;
const MAX_SPEED = 76;
const HIGH_SCORE_KEY = 'space-zone-space-runner-high-score';
const OBSTACLE_EMOJI: Record<ObstacleKind, string> = { meteor: '☄️', barrier: '🧱', drone: '🤖' };
const PICKUP_EMOJI: Record<PickupKind, string> = { coin: '🪙', star: '⭐', energy: '⚡' };

function clampLane(value: number): Lane { return Math.max(0, Math.min(2, value)) as Lane; }
function getHighScore() { if (typeof window === 'undefined') return 0; const saved = Number(window.localStorage.getItem(HIGH_SCORE_KEY)); return Number.isFinite(saved) ? saved : 0; }

export default function SpaceRunner({ onClose, onWin }: GameProps) {
  const [lane, setLane] = useState<Lane>(1); const [obstacles, setObstacles] = useState<Obstacle[]>([]); const [pickups, setPickups] = useState<Pickup[]>([]); const [bursts, setBursts] = useState<Burst[]>([]);
  const [distance, setDistance] = useState(0); const [coins, setCoins] = useState(0); const [time, setTime] = useState(GAME_LENGTH); const [speed, setSpeed] = useState(START_SPEED);
  const [jumping, setJumping] = useState(false); const [sliding, setSliding] = useState(false); const [shield, setShield] = useState(false); const [boost, setBoost] = useState(0); const [highScore, setHighScore] = useState(getHighScore); const [state, setState] = useState<RunnerState>('playing');
  const laneRef = useRef<Lane>(1); const obstaclesRef = useRef<Obstacle[]>([]); const pickupsRef = useRef<Pickup[]>([]); const lastFrameRef = useRef(0); const spawnRef = useRef(0); const pickupSpawnRef = useRef(0); const idRef = useRef(0);
  const distanceRef = useRef(0); const coinsRef = useRef(0); const boostRef = useRef(0); const shieldRef = useRef(false); const jumpingRef = useRef(false); const slidingRef = useRef(false); const resultSentRef = useRef(false); const touchStartX = useRef<number | null>(null); const touchStartY = useRef<number | null>(null);
  laneRef.current = lane; obstaclesRef.current = obstacles; pickupsRef.current = pickups; distanceRef.current = distance; coinsRef.current = coins; boostRef.current = boost; shieldRef.current = shield; jumpingRef.current = jumping; slidingRef.current = sliding;

  const finish = useCallback((won: boolean) => { if (resultSentRef.current) return; resultSentRef.current = true; const finalScore = Math.floor(distanceRef.current) + coinsRef.current * 25; const nextHigh = Math.max(highScore, finalScore); if (typeof window !== 'undefined') window.localStorage.setItem(HIGH_SCORE_KEY, String(nextHigh)); setHighScore(nextHigh); setState(won ? 'won' : 'lost'); onWin(won ? (coinsRef.current >= 16 ? 3 : coinsRef.current >= 9 ? 2 : 1) : 0); }, [highScore, onWin]);
  const reset = useCallback(() => { laneRef.current=1; obstaclesRef.current=[]; pickupsRef.current=[]; distanceRef.current=0; coinsRef.current=0; boostRef.current=0; shieldRef.current=false; jumpingRef.current=false; slidingRef.current=false; resultSentRef.current=false; lastFrameRef.current=0; spawnRef.current=0; pickupSpawnRef.current=0; idRef.current=0; setLane(1); setObstacles([]); setPickups([]); setBursts([]); setDistance(0); setCoins(0); setTime(GAME_LENGTH); setSpeed(START_SPEED); setJumping(false); setSliding(false); setShield(false); setBoost(0); setState('playing'); }, []);
  const move = useCallback((direction:-1|1)=>{if(state!=='playing')return;const next=clampLane(laneRef.current+direction);laneRef.current=next;setLane(next);},[state]);
  const jump = useCallback(()=>{if(state!=='playing'||jumpingRef.current||slidingRef.current)return;jumpingRef.current=true;setJumping(true);window.setTimeout(()=>{jumpingRef.current=false;setJumping(false);},820);},[state]);
  const slide = useCallback(()=>{if(state!=='playing'||jumpingRef.current||slidingRef.current)return;slidingRef.current=true;setSliding(true);window.setTimeout(()=>{slidingRef.current=false;setSliding(false);},650);},[state]);

  useEffect(()=>{const onKeyDown=(event:KeyboardEvent)=>{if(event.repeat)return;if(event.key==='ArrowLeft'||event.key.toLowerCase()==='a')move(-1);else if(event.key==='ArrowRight'||event.key.toLowerCase()==='d')move(1);else if(event.key==='ArrowUp'||event.key.toLowerCase()==='w'||event.key===' '){event.preventDefault();jump();}else if(event.key==='ArrowDown'||event.key.toLowerCase()==='s')slide();else if(event.key.toLowerCase()==='p')setState(current=>current==='playing'?'paused':current==='paused'?'playing':current);};window.addEventListener('keydown',onKeyDown);return()=>window.removeEventListener('keydown',onKeyDown);},[jump,move,slide]);
  useEffect(()=>{if(state!=='playing')return;let frame=0;const loop=(now:number)=>{const dt=Math.min(.034,lastFrameRef.current?(now-lastFrameRef.current)/1000:.016);lastFrameRef.current=now;const effectiveSpeed=Math.min(MAX_SPEED,speed+(boostRef.current>0?29:0));const travel=effectiveSpeed*dt;const nextDistance=distanceRef.current+travel;distanceRef.current=nextDistance;setDistance(nextDistance);setBoost(v=>Math.max(0,v-dt));boostRef.current=Math.max(0,boostRef.current-dt);if(Math.random()<dt*1.3)setSpeed(v=>Math.min(MAX_SPEED,v+.28));spawnRef.current-=dt;if(spawnRef.current<=0){const busy=new Set(obstaclesRef.current.filter(i=>i.y>72).map(i=>i.lane));const available=LANES.filter(i=>!busy.has(i));const spawnLane=available[Math.floor(Math.random()*Math.max(1,available.length))]??(Math.floor(Math.random()*3)as Lane);const kind:ObstacleKind=Math.random()<.34?'drone':Math.random()<.5?'meteor':'barrier';obstaclesRef.current=[...obstaclesRef.current,{id:idRef.current++,lane:spawnLane,y:0,kind,phase:Math.random()*Math.PI*2}];spawnRef.current=Math.max(.42,.96-(speed-START_SPEED)/115)+Math.random()*.5;}pickupSpawnRef.current-=dt;if(pickupSpawnRef.current<=0){const pickupLane=Math.floor(Math.random()*3)as Lane;const kind:PickupKind=Math.random()<.16?'energy':Math.random()<.3?'star':'coin';pickupsRef.current=[...pickupsRef.current,{id:idRef.current++,lane:pickupLane,y:0,kind,phase:Math.random()*Math.PI*2}];pickupSpawnRef.current=.38+Math.random()*.52;}let missedObstacle=false;let nextObstacles=obstaclesRef.current.map(i=>({...i,y:i.y+travel*2.15,phase:i.phase+dt*3})).filter(i=>i.y<118);const hitIds=new Set<number>();for(const item of nextObstacles)if(item.y>87&&item.y<103&&item.lane===laneRef.current){const safe=item.kind==='drone'?jumpingRef.current:item.kind==='barrier'?slidingRef.current:jumpingRef.current||slidingRef.current;if(!safe){hitIds.add(item.id);if(shieldRef.current){shieldRef.current=false;setShield(false);setBursts(items=>[...items,{id:item.id,lane:item.lane,y:item.y,emoji:'🛡️'}]);}else missedObstacle=true;}}nextObstacles=nextObstacles.filter(i=>!hitIds.has(i.id));let nextPickups=pickupsRef.current.map(i=>({...i,y:i.y+travel*2.2,phase:i.phase+dt*5})).filter(i=>i.y<118);const pickupHits=new Set<number>();for(const item of nextPickups)if(item.y>87&&item.y<104&&item.lane===laneRef.current){pickupHits.add(item.id);if(item.kind==='coin'){coinsRef.current+=1;setCoins(coinsRef.current);}else if(item.kind==='star'){coinsRef.current+=2;setCoins(coinsRef.current);shieldRef.current=true;setShield(true);}else{boostRef.current=3.4;setBoost(3.4);}setBursts(items=>[...items,{id:item.id,lane:item.lane,y:item.y,emoji:PICKUP_EMOJI[item.kind]}]);}nextPickups=nextPickups.filter(i=>!pickupHits.has(i.id));obstaclesRef.current=nextObstacles;pickupsRef.current=nextPickups;setObstacles(nextObstacles);setPickups(nextPickups);setBursts(items=>items.slice(-8).map(i=>({...i,y:i.y+travel*.8})).filter(i=>i.y<124));if(missedObstacle||nextDistance>=GAME_LENGTH*10){finish(!missedObstacle);return;}frame=requestAnimationFrame(loop);};frame=requestAnimationFrame(loop);return()=>cancelAnimationFrame(frame);},[finish,speed,state]);
  useEffect(()=>{if(state!=='playing')return;const timer=window.setInterval(()=>{setTime(value=>{const next=value-1;if(next<=0)finish(true);return Math.max(0,next);});},1000);return()=>window.clearInterval(timer);},[finish,state]);
  const onTouchStart=(event:React.TouchEvent<HTMLDivElement>)=>{touchStartX.current=event.touches[0]?.clientX??null;touchStartY.current=event.touches[0]?.clientY??null;};
  const onTouchEnd=(event:React.TouchEvent<HTMLDivElement>)=>{if(touchStartX.current===null||touchStartY.current===null)return;const dx=(event.changedTouches[0]?.clientX??touchStartX.current)-touchStartX.current;const dy=(event.changedTouches[0]?.clientY??touchStartY.current)-touchStartY.current;touchStartX.current=null;touchStartY.current=null;if(Math.max(Math.abs(dx),Math.abs(dy))<26)return;if(Math.abs(dx)>Math.abs(dy))move(dx>0?1:-1);else if(dy<0)jump();else slide();};
  const score=Math.floor(distance)+coins*25;const progress=Math.min(100,distance/(GAME_LENGTH*10)*100);const roadStyle=(laneIndex:Lane):CSSProperties=>({left:`${17+laneIndex*33}%`});

  return <GameShell title="SPACE RUNNER" gradient="from-sky-300 to-lavender-300" emoji="🚀" onClose={onClose} onRestart={reset} status={state==='paused'?'playing':state} stars={coins>=16?3:coins>=9?2:1} winMessage="COSMIC CHAMPION!" loseMessage="Crash Landing!" winDetail={`Distance ${Math.floor(distance)} • Score ${score} • Coins ${coins}`} wide fullscreen stats={[{icon:'clock',value:`${time}s`,color:'text-sky-500'},{icon:'score',value:`${score}`,color:'text-lavender-500'},{icon:'star',value:`${coins}`,color:'text-lemon-500'}]}>
    <div className="h-full min-h-0 select-none" onTouchStart={onTouchStart} onTouchEnd={onTouchEnd}>
      <div className="mx-auto mb-3 grid max-w-[1500px] gap-3 sm:grid-cols-[1fr_auto] sm:items-center"><div className="rounded-2xl border border-white/70 bg-white/55 p-3 shadow-sm backdrop-blur-md"><div className="flex items-center justify-between gap-3 text-xs font-display font-bold text-lavender-400"><span className="inline-flex items-center gap-1.5"><Rocket size={14}/> Cosmic route</span><span className="text-sky-500">720m</span></div><div className="mt-2 h-2.5 overflow-hidden rounded-full bg-lavender-100"><motion.div className="h-full rounded-full bg-gradient-to-r from-sky-400 via-cyan-300 to-lavender-400" animate={{width:`${progress}%`}} transition={{duration:.2}}/></div></div><div className="grid grid-cols-2 gap-2 sm:min-w-[220px]"><div className="rounded-2xl border border-white/70 bg-white/55 px-4 py-3 text-center shadow-sm backdrop-blur-md"><p className="text-[10px] font-bold uppercase tracking-wider text-lavender-300">Speed</p><p className="font-display text-lg font-black text-sky-500">{Math.round(speed)}<span className="text-xs"> km/h</span></p></div><div className="rounded-2xl border border-white/70 bg-white/55 px-4 py-3 text-center shadow-sm backdrop-blur-md"><p className="text-[10px] font-bold uppercase tracking-wider text-lavender-300">Power</p><p className="font-display text-lg font-black text-lavender-500">{shield?'SHIELD':boost>0?'BOOST':'READY'}</p></div></div></div>
      <div className="relative mx-auto h-[calc(100vh-225px)] min-h-[440px] max-h-[calc(100vh-225px)] w-full max-w-[1500px] overflow-hidden rounded-[2.5rem] border border-cyan-200/20 bg-[#050b1a] shadow-[inset_0_0_100px_rgba(56,189,248,.12),0_30px_90px_rgba(2,8,23,.35)]">
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_5%,rgba(56,189,248,.34),transparent_28%),radial-gradient(circle_at_10%_88%,rgba(124,92,231,.25),transparent_35%),linear-gradient(180deg,#061022_0%,#0b1732_48%,#111f46_100%)]"/><div className="absolute inset-0 opacity-45 [background-image:linear-gradient(rgba(255,255,255,.025)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,.025)_1px,transparent_1px)] [background-size:56px_56px]"/>
        {Array.from({length:34}).map((_,index)=><motion.span key={index} className="absolute rounded-full bg-white" style={{left:`${(index*37)%98}%`,top:`${3+((index*19)%80)}%`,width:index%5===0?3:2,height:index%5===0?3:2}} animate={{opacity:[.12,.9,.12],scale:[.65,1.3,.65]}} transition={{duration:1.5+(index%4)*.4,repeat:Infinity,delay:index*.05}}/>)}
        <div className="absolute inset-x-4 bottom-[15%] top-[12%] overflow-hidden rounded-t-[50%] border-x border-white/10 bg-gradient-to-b from-slate-950/10 via-indigo-950/20 to-slate-950/75 sm:inset-x-[9%]"><div className="absolute inset-0 bg-[linear-gradient(90deg,transparent_31.8%,rgba(148,163,184,.25)_32%,rgba(148,163,184,.25)_32.4%,transparent_32.8%,transparent_65.1%,rgba(148,163,184,.25)_65.4%,rgba(148,163,184,.25)_65.8%,transparent_66%)]"/><motion.div className="absolute inset-0 bg-[repeating-linear-gradient(180deg,transparent_0,transparent_32px,rgba(125,211,252,.12)_33px,rgba(125,211,252,.12)_36px)]" animate={{backgroundPositionY:['0px','72px']}} transition={{duration:.9,repeat:Infinity,ease:'linear'}}/>{LANES.map(laneIndex=><div key={laneIndex} className="absolute bottom-0 top-0 w-[27%] border-x border-white/5" style={roadStyle(laneIndex)}/>)}
          {obstacles.map(item=><motion.div key={item.id} className="absolute z-20 flex h-16 w-16 -translate-x-1/2 -translate-y-1/2 items-center justify-center rounded-2xl border border-white/20 bg-white/10 text-4xl shadow-[0_0_34px_rgba(248,113,113,.22)] backdrop-blur-md" style={{left:`${17+item.lane*33}%`,top:`${item.y}%`}} animate={{rotate:[0,item.kind==='meteor'?12:-6,0],scale:[.9,1.06,.9]}} transition={{duration:.5,repeat:Infinity}}>{OBSTACLE_EMOJI[item.kind]}</motion.div>)}
          {pickups.map(item=><motion.div key={item.id} className="absolute z-10 flex h-12 w-12 -translate-x-1/2 -translate-y-1/2 items-center justify-center rounded-full border border-yellow-200/20 bg-yellow-200/10 text-3xl shadow-[0_0_34px_rgba(250,204,21,.28)] backdrop-blur-md" style={{left:`${17+item.lane*33}%`,top:`${item.y}%`}} animate={{y:[-5,5,-5],rotate:[0,180,360]}} transition={{duration:1.1,repeat:Infinity}}>{PICKUP_EMOJI[item.kind]}</motion.div>)}
          {bursts.map(burst=><motion.div key={burst.id} className="pointer-events-none absolute z-30 -translate-x-1/2 -translate-y-1/2 text-2xl" style={{left:`${17+burst.lane*33}%`,top:`${burst.y}%`}} initial={{opacity:1,scale:.5}} animate={{opacity:0,scale:1.9,y:-40}} transition={{duration:.55}}>{burst.emoji}</motion.div>)}
          <motion.div className="absolute z-40 -translate-x-1/2" style={{left:`${17+lane*33}%`,bottom:jumping?'21%':sliding?'4%':'6%'}} animate={{y:jumping?[0,-90,0]:0,rotate:jumping?[-8,8,-8]:0}} transition={{duration:.82,ease:'easeOut'}}><div className={`${sliding?'h-14 w-24':'h-24 w-24'} relative flex items-center justify-center rounded-[1.8rem] border border-sky-100/45 bg-gradient-to-br from-sky-200/55 to-violet-300/45 shadow-[0_0_50px_rgba(56,189,248,.4)] backdrop-blur-lg`}><div className="absolute -bottom-4 h-8 w-16 rounded-full bg-sky-400/25 blur-xl"/><Rocket className="text-sky-100" size={sliding?42:60} strokeWidth={1.5}/><motion.span className="absolute -bottom-7 text-xl" animate={{scaleY:[1,1.6,1]}} transition={{duration:.16,repeat:Infinity}}>🔥</motion.span>{shield&&<motion.div className="absolute inset-[-11px] rounded-[2rem] border-2 border-yellow-200/75" animate={{opacity:[.35,.95,.35],scale:[.97,1.04,.97]}} transition={{duration:1.05,repeat:Infinity}}/>}</div></motion.div>
        </div>
        <div className="absolute bottom-4 left-4 right-4 z-40 grid grid-cols-2 gap-2 sm:left-7 sm:right-7 sm:bottom-6 sm:grid-cols-4">{[{v:Math.floor(distance)+'m',l:'Distance'},{v:String(coins),l:'Coins'},{v:highScore.toLocaleString(),l:'Best'},{v:shield?'SHIELD':boost>0?`BOOST ${boost.toFixed(1)}s`:'READY',l:'Power'}].map(item=><div key={item.l} className="rounded-2xl border border-white/10 bg-white/5 px-3 py-2.5 backdrop-blur-xl"><p className="text-[9px] font-bold uppercase tracking-wider text-slate-400">{item.l}</p><p className="font-display text-base font-black text-white">{item.v}</p></div>)}</div>
        {state==='paused'&&<div className="absolute inset-0 z-50 flex items-center justify-center bg-slate-950/70 backdrop-blur-sm"><div className="text-center"><Pause size={52} className="mx-auto text-sky-200"/><p className="mt-3 font-display text-3xl font-black text-white">PAUSED</p><button type="button" onClick={()=>setState('playing')} className="mt-5 inline-flex items-center gap-2 rounded-2xl bg-white px-6 py-3 font-bold text-indigo-950 shadow-lg"><Play size={17}/> Continue</button></div></div>}
      </div>
      <div className="mx-auto mt-3 grid max-w-[900px] grid-cols-4 gap-2"><button type="button" onClick={()=>move(-1)} className="flex min-h-12 items-center justify-center gap-1.5 rounded-2xl border border-white/80 bg-white/75 px-2 text-xs font-display font-bold text-lavender-500 shadow-sm active:scale-95 sm:text-sm"><ArrowLeft size={18}/> Left</button><button type="button" onClick={jump} className="flex min-h-12 items-center justify-center gap-1.5 rounded-2xl border border-white/80 bg-white/75 px-2 text-xs font-display font-bold text-lavender-500 shadow-sm active:scale-95"><Rocket size={18}/> Jump</button><button type="button" onClick={slide} className="flex min-h-12 items-center justify-center gap-1.5 rounded-2xl border border-white/80 bg-white/75 px-2 text-xs font-display font-bold text-lavender-500 shadow-sm active:scale-95"><ArrowDown size={18}/> Slide</button><button type="button" onClick={()=>move(1)} className="flex min-h-12 items-center justify-center gap-1.5 rounded-2xl border border-white/80 bg-white/75 px-2 text-xs font-display font-bold text-lavender-500 shadow-sm active:scale-95">Right <ArrowRight size={18}/></button></div>
      <div className="mt-2 flex flex-wrap items-center justify-center gap-3 text-[11px] font-semibold text-lavender-300 sm:text-xs"><span>⌨️ ← → move</span><span>⬆️ / SPACE jump</span><span>⬇️ slide</span><span>📱 swipe</span><span>P pause</span></div>
      <div className="mt-2 flex items-center justify-center gap-2 text-xs font-semibold text-sky-500"><Sparkles size={14}/> Collect ⭐ for Shield and ⚡ for Turbo!</div>
    </div>
  </GameShell>;
}
